import 'package:flutter/material.dart';

class MovieTrailer extends StatelessWidget {
  final int movieId;

  MovieTrailer({this.movieId, Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
